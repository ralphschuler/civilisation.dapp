name: Release Notes

on:
  release:
    types:
      - created
      - edited
  workflow_dispatch:

permissions:
  contents: write
  releases: write
  models: read

env:
  USE_GITHUB_MODELS: "true"

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Load Release Notes Prompt
        id: load_prompt
        run: echo "$(cat .github/prompts/release_notes.md)" > prompt.txt

      - name: Generate Release Notes
        id: generate_notes
        run: |
          echo "Generating release notes using LLM..."
          # Placeholder for LLM call

      - name: Update Release Body
        run: |
          echo "Updating release body..."
          # Placeholder for updating release body